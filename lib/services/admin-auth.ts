import "server-only";
import { createClient } from "@/lib/supabase/server";
export async function requireAdmin(options?: { allowPasswordChange?: boolean }) { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) throw new Error("UNAUTHORIZED"); const { data: admin } = await supabase.from("admin_users").select("user_id,role,active,must_change_password,display_name,team").eq("user_id", user.id).eq("active", true).maybeSingle(); if (!admin) throw new Error("FORBIDDEN"); if (admin.must_change_password && !options?.allowPasswordChange) throw new Error("PASSWORD_CHANGE_REQUIRED"); return { supabase, admin, user }; }
