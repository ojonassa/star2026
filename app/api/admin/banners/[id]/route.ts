import { NextResponse } from "next/server";
import { z } from "zod";
import { requireAdmin } from "@/lib/services/admin-auth";
const schema = z.object({ title: z.string().min(2).max(160), subtitle: z.string().max(300).nullable(), image_url: z.string().min(1).max(500), link_url: z.string().url().nullable(), open_in_new_tab: z.boolean(), sort_order: z.number().int().min(0), active: z.boolean() });
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { const input = schema.safeParse(await request.json().catch(() => null)); if (!input.success) return NextResponse.json({ message: "Banner inválido." }, { status: 400 }); try { const { supabase, admin } = await requireAdmin(); const { id } = await params; const { error } = await supabase.from("banners").update(input.data).eq("id", id); if (error) throw error; await supabase.from("audit_logs").insert({ admin_user_id: admin.user_id, action: "update", entity_type: "banner", entity_id: id }); return NextResponse.json({ message: "Banner atualizado." }); } catch { return NextResponse.json({ message: "Não foi possível atualizar o banner." }, { status: 403 }); } }
export async function DELETE(_: Request, { params }: { params: Promise<{ id: string }> }) { try { const { supabase, admin } = await requireAdmin(); const { id } = await params; const { error } = await supabase.from("banners").delete().eq("id", id); if (error) throw error; await supabase.from("audit_logs").insert({ admin_user_id: admin.user_id, action: "delete", entity_type: "banner", entity_id: id }); return NextResponse.json({ message: "Banner excluído." }); } catch { return NextResponse.json({ message: "Não foi possível excluir o banner." }, { status: 403 }); } }
