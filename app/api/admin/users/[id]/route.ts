import { NextResponse } from "next/server";
import { z } from "zod";
import { createAdminClient } from "@/lib/supabase/admin";
import { requireAdmin } from "@/lib/services/admin-auth";
const password = z.string().min(10).max(128).refine(x => /[A-Z]/.test(x) && /[a-z]/.test(x) && /\d/.test(x), "Senha fraca");
const schema = z.object({ displayName: z.string().trim().min(3).max(120), email: z.string().trim().email().max(254), team: z.enum(["coordination", "development", "marketing", "credentialing", "support"]), active: z.boolean(), password: password.optional(), temporaryPassword: password.optional() });
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { const input = schema.safeParse(await request.json().catch(() => null)); if (!input.success) return NextResponse.json({ message: "Dados inválidos ou senha fraca." }, { status: 400 }); try { const { id } = await params; const { admin } = await requireAdmin(); if (id === admin.user_id && !input.data.active) return NextResponse.json({ message: "Você não pode desativar sua própria conta." }, { status: 400 }); const service = createAdminClient(); const nextPassword = input.data.password ?? input.data.temporaryPassword; const { error: authError } = await service.auth.admin.updateUserById(id, { email: input.data.email.toLowerCase(), password: nextPassword }); if (authError) throw authError; const { error } = await service.from("admin_users").update({ display_name: input.data.displayName, email: input.data.email.toLowerCase(), team: input.data.team, active: input.data.active, must_change_password: false }).eq("user_id", id); if (error) throw error; await service.from("audit_logs").insert({ admin_user_id: admin.user_id, action: nextPassword ? "change_admin_password" : input.data.active ? "update_admin_user" : "deactivate_admin_user", entity_type: "admin_user", entity_id: id }); return NextResponse.json({ message: nextPassword ? "Usuário e senha atualizados." : "Usuário atualizado." }); } catch { return NextResponse.json({ message: "Não foi possível atualizar o usuário." }, { status: 403 }); } }
export async function DELETE(_: Request, { params }: { params: Promise<{ id: string }> }) { try { const { id } = await params; const { admin } = await requireAdmin(); const service = createAdminClient(); const { data: first } = await service.from("admin_users").select("user_id").order("created_at", { ascending: true }).limit(1).maybeSingle(); if (!first || first.user_id !== admin.user_id) return NextResponse.json({ message: "Somente o primeiro usuário administrativo pode excluir contas." }, { status: 403 }); if (id === admin.user_id) return NextResponse.json({ message: "Você não pode excluir sua própria conta." }, { status: 400 }); const { count } = await service.from("admin_users").select("user_id", { count: "exact", head: true }).eq("active", true); const { data: target } = await service.from("admin_users").select("active").eq("user_id", id).maybeSingle(); if (!target) return NextResponse.json({ message: "Usuário não encontrado." }, { status: 404 }); if (target.active && (count ?? 0) <= 1) return NextResponse.json({ message: "Não é permitido excluir o último administrador ativo." }, { status: 400 }); await service.from("audit_logs").insert({ admin_user_id: admin.user_id, action: "delete_admin_user", entity_type: "admin_user", entity_id: id }); const { error } = await service.auth.admin.deleteUser(id); if (error) throw error; return NextResponse.json({ message: "Usuário excluído e acesso removido." }); } catch { return NextResponse.json({ message: "Não foi possível excluir o usuário." }, { status: 500 }); } }
