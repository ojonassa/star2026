"use client";
import { useRouter } from "next/navigation";
import { useState } from "react";
export default function ChangePasswordPage() { const router = useRouter(); const [message, setMessage] = useState(""); const [busy, setBusy] = useState(false); async function submit(form: FormData) { const password = String(form.get("password")); if (password !== String(form.get("confirmation"))) { setMessage("As senhas não coincidem."); return; } setBusy(true); const response = await fetch("/api/admin/change-password", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ password }) }); const body = await response.json(); setMessage(body.message); setBusy(false); if (response.ok) { router.replace("/admin"); router.refresh(); } } return <main className="mx-auto max-w-md py-12"><h1 className="text-3xl font-bold">Altere sua senha</h1><p className="mt-2 text-slate-600">Por segurança, a senha temporária deve ser substituída antes de acessar o painel.</p><form action={submit} className="mt-6 space-y-4 rounded-xl border bg-white p-5"><label className="block">Nova senha<input required minLength={10} name="password" type="password" className="mt-1 block w-full rounded border p-2"/></label><label className="block">Confirmar nova senha<input required minLength={10} name="confirmation" type="password" className="mt-1 block w-full rounded border p-2"/></label>{message && <p role="status" className="rounded border border-emerald-200 bg-emerald-50 p-3">{message}</p>}<button disabled={busy} className="rounded bg-slate-900 px-4 py-2 text-white">{busy ? "Salvando…" : "Salvar nova senha"}</button></form></main>; }
