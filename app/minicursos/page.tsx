import Link from "next/link";
import { getPublicEventData } from "@/lib/services/public-event";
export default async function CoursesPage() { const { courses } = await getPublicEventData(); return <main className="mx-auto min-h-screen max-w-5xl px-4 py-10"><Link href="/" className="text-blue-700 hover:underline">Voltar</Link><h1 className="mt-6 text-3xl font-bold">Minicursos</h1>{courses.length ? <ul className="mt-6 grid gap-4 md:grid-cols-2">{courses.map(c => <li key={c.id} className="rounded border p-5"><h2 className="font-bold">{c.title}</h2><p className="mt-2 text-sm text-slate-600">{c.description}</p><Link className="mt-4 inline-block text-blue-700 hover:underline" href={`/minicursos/${c.slug}`}>Ver detalhes</Link></li>)}</ul> : <p className="mt-6">Nenhum minicurso publicado no momento.</p>}</main>; }
