"use client";
import { useState } from "react";
export function CourseEnrollmentForm({ courseId }: { courseId: string }) { const [message, setMessage] = useState(""); const [pending, setPending] = useState(false); async function submit(formData: FormData) { setPending(true); setMessage(""); const response = await fetch("/api/course-registrations", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ courseId, cpf: formData.get("cpf"), birthDate: formData.get("birthDate") }) }); const body = await response.json(); setMessage(body.message); setPending(false); } return <form action={submit} className="mt-6 space-y-3 rounded-xl border p-4"><h2 className="font-bold">Matricule-se</h2><label className="block text-sm">CPF<input name="cpf" required className="mt-1 block w-full rounded border p-2" /></label><label className="block text-sm">Data de nascimento<input name="birthDate" type="date" required className="mt-1 block w-full rounded border p-2" /></label>{message && <p role="status">{message}</p>}<button disabled={pending} className="rounded bg-blue-700 px-4 py-2 text-white disabled:opacity-50">{pending ? "Enviando…" : "Confirmar matrícula"}</button></form>; }
