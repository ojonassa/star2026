"use client";
/* eslint-disable @next/next/no-img-element */
import { useRef } from "react";
type Banner = { id: string; title: string; subtitle: string | null; image_url: string; link_url: string | null; open_in_new_tab: boolean };
export function BannerCarousel({ banners }: { banners: Banner[] }) { const ref = useRef<HTMLDivElement>(null); const move = (direction: number) => ref.current?.scrollBy({ left: direction * (ref.current.clientWidth || 1), behavior: "smooth" }); return <div className="relative"><div ref={ref} className="flex snap-x snap-mandatory overflow-x-auto scroll-smooth rounded-xl" aria-label="Banners do evento">{banners.map(b => <a key={b.id} href={b.link_url ?? undefined} target={b.open_in_new_tab ? "_blank" : undefined} rel={b.open_in_new_tab ? "noreferrer" : undefined} className="relative min-w-full snap-start bg-slate-900 text-white focus:outline-none focus:ring-4 focus:ring-blue-400"><img src={b.image_url.startsWith("http") ? b.image_url : `/api/media/${b.image_url}`} alt={b.title || "Banner do evento"} className="h-52 w-full object-cover sm:h-80" /><span className="absolute inset-x-0 bottom-0 bg-gradient-to-t from-black/80 p-5"><strong className="block text-lg">{b.title}</strong>{b.subtitle && <span>{b.subtitle}</span>}</span></a>)}</div><div className="mt-3 flex justify-center gap-2"><button type="button" onClick={() => move(-1)} className="rounded border px-3 py-1">Anterior</button><button type="button" onClick={() => move(1)} className="rounded border px-3 py-1">Próximo</button></div></div>; }
