import { describe, expect, it } from "vitest";
import { isValidCpf, normalizeCpf } from "../lib/validation/cpf";
import { checkinResult, enrollmentResult } from "../lib/services/critical-rules";
describe("CPF", () => { it("normaliza e valida CPF", () => { expect(normalizeCpf("529.982.247-25")).toBe("52998224725"); expect(isValidCpf("529.982.247-25")).toBe(true); expect(isValidCpf("111.111.111-11")).toBe(false); }); });
describe("regras críticas", () => { const a = { participantId: "p", courseId: "a", startsAt: new Date("2026-01-01T10:00Z"), endsAt: new Date("2026-01-01T12:00Z") }; it("impede matrícula duplicada, lotação e conflito", () => { expect(enrollmentResult([a], a, 2)).toBe("already_registered"); expect(enrollmentResult([a], { ...a, participantId: "b" }, 1)).toBe("full"); expect(enrollmentResult([a], { ...a, courseId: "b", startsAt: new Date("2026-01-01T11:00Z"), endsAt: new Date("2026-01-01T13:00Z") }, 2)).toBe("schedule_conflict"); }); it("autentica CPF+nascimento representado pelo participante válido e torna check-in idempotente", () => { const seen = new Set<string>(); expect(checkinResult(seen, true, true, "day:p")).toBe("checked_in"); expect(checkinResult(seen, true, true, "day:p")).toBe("already_checked_in"); expect(checkinResult(seen, false, true, "day:q")).toBe("invalid_token"); expect(checkinResult(seen, true, false, "day:q")).toBe("participant_not_found"); }); });
